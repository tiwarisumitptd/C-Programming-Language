#include<stdio.h>
int sum (int x, int y) {
  int c = x + y;
  printf ("sum of a and b is: \n");
  return c;
}
int main () {
  int a;
  printf ("Enter the value of a:");
  scanf ("%d", &a);
  int b;
  printf ("Enter the value of b:");
  scanf ("%d", &b);
  int sum (int a, int b);
  return 0;
}