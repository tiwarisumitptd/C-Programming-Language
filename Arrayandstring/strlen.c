#include<stdio.h>
#include<string.h>
int main () {
   char str [] = "Hello";
   printf ("The given string is: %s", str);
   int len = strlen (str);
   printf ("\nThe length of given string is: %d", len);
   return 0;
}
